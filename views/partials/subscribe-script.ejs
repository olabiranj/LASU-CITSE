<script>
    $(document).ready(function() {
$('form').on('submit', function(e) {
    e.preventDefault();

    // cache the current form so you make sure to only have data from this one
    var form = this,
        $form = $(form);

    $.ajax({
        url: form.action,
        type: form.method,
        data: $form.serialize(),
        success: function(data) {
            if (data == 'true')
            {
                $form.attr('action', req.originalUrl).off('submit').submit();
            }
            else
            {
                alert('thanks for subscribing to our news letter');
            }
        },
        error: function() {
            alert('There has been an error, please alert us immediately');
        }
    });
});
});
        
    </script>